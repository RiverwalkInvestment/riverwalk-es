// GSAP Interactive — no Draggable needed; drag handled in gsap-animations.js
// Accordions are pure CSS. This module is a no-op kept for module compatibility.

export function initInteractive() {
  // Intentionally empty — drag scroll is in gsap-animations.js
  // Pure CSS accordions need no JS
}
